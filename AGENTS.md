# 仓库工作约定

- 仅在当前项目目录内读写文件，不探查项目外的指令或配置。
- 按文件或小模块逐步修改、检查；不要一次并行修改大量文件。
- 使用 Bun 管理依赖和运行脚本，版本遵循根 `package.json`；保留 `bun.lock`，不要改用 npm。
- 数据库访问统一使用 Drizzle ORM，通过 `apps/api/src/db` 初始化与定义表结构；业务代码不直接调用 D1 prepare/exec 或手写 SQL 查询。
- 本文件仅保留基本约束和偏好。项目进展、待办、阻塞、验证结果及技术交接信息统一写入根目录 `HANDOFF.md`。
- 接手任务前阅读 `HANDOFF.md`；工作告一段落时更新交接，明确区分已编写、已验证和待完成事项。
